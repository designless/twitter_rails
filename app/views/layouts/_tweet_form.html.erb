<%= form_for(@tweet) do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new tweet..." %>
  </div>
  <%= f.submit "Tweet", class: "btn btn-large btn-primary" %>
<% end %>

